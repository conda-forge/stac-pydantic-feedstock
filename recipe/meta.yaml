{% set version = "3.5.2" %}

package:
  name: stac-pydantic
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/s/stac-pydantic/stac_pydantic-{{ version }}.tar.gz
  sha256: 72dd366674348f1f1fffa2e627db58b3582bd7fa275d15fd190f114c4552ee89

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  entry_points:
    - stac-pydantic = stac_pydantic.scripts.cli:app

requirements:
  host:
    - python {{ python_min }}
    - pip
    - hatchling
  run:
    - python >={{ python_min }}
    - click >=8.1.7
    - pydantic >=2.4.1
    - geojson-pydantic >=1.0.0
test:
  imports:
    - stac_pydantic
  commands:
    - pip check
    - stac-pydantic --help
  requires:
    - pip
    - python {{ python_min }}
    - requests

about:
  home: https://github.com/CS-SI/stac-pydantic
  summary: Pydantic data models for the STAC spec
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - gadomski
    - sbrunato
